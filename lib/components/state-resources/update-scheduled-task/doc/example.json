{
  "UpdateScheduledTask": {
    "Type": "Task",
    "Parameters": {
      "key.$": "$.key",
      "scheduleType.$": "$.scheduleType",
      "datetime.$": "$.datetime",
      "interval.$": "$.interval"
    },
    "Resource": "module:updateScheduledTask",
    "End": true
  }
}
